# Ejar frontend

This folder contains the React (Vite + TypeScript) frontend.

- Main project docs: `../README.md`
- Start dev server:

```zsh
npm install
cp .env.example .env  # optional
npm run dev
```
