// Notification store functionality is handled directly through useNotifications hook
// No separate store needed - state is managed locally in the component
// See: hooks/useNotifcations.ts for notification state management
